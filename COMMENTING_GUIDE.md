# استاندارد اجباری کامنت‌گذاری پروژه

این قانون برای تمام نسخه‌های آینده App BotStore الزامی است.

1. ابتدای هر فایل دست‌نویس توضیح داده شود فایل برای چه ساخته شده است.
2. بالای هر class، object، enum و data class وظیفه آن نوشته شود.
3. بالای هر function و Composable ورودی، خروجی و هدف آن توضیح داده شود.
4. stateها، متغیرهای مهم و callbackها هنگام تعریف توضیح داشته باشند.
5. قبل از عملیات مهم مانند ذخیره اطلاعات، درخواست شبکه، تغییر صفحه، حذف داده، ورود، پرداخت و امضا توضیح فارسی نوشته شود.
6. تنظیمات Gradle، Manifest، Workflow و Resourceهای دست‌نویس نیز کامنت‌گذاری شوند.
7. کد تولیدشده خودکار، خروجی Build و فایل‌های باینری نیاز به کامنت ندارند.
8. کامنت باید توضیح بدهد «چرا و چه کاری» انجام می‌شود و صرفاً ترجمه نام دستور نباشد.
9. هنگام تغییر رفتار یک بخش، کامنت همان بخش نیز باید همزمان بروزرسانی شود.
10. هیچ قابلیت جدیدی بدون کامنت توضیحی وارد شاخه اصلی نشود.

فایل‌های اصلی موجود پروژه با همین الگو مستندسازی شده‌اند و توسعه‌های بعدی باید همین سطح توضیح را حفظ کنند.
